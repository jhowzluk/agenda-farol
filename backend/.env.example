PORT=5000
# Para desenvolvimento local, qualquer texto serve como segredo.
# Para produção, gere uma chave forte (ex: openssl rand -base64 32)
JWT_SECRET=farol_agenda_desenvolvimento_secret_key_12345

# DATABASE_URL: String de conexão MySQL/TiDB.
# Formato: mysql://usuario:senha@host:porta/nome_do_banco
# Deixe esta linha comentada para usar o MySQL local padrão (mysql://root@localhost:3306/agenda_farol)
# DATABASE_URL=mysql://usuario:senha@host:porta/nome_do_banco
